language: python
python:
  - "2.7"

env:
  - DJANGO=1.4.3
  - DJANGO=1.3.5
  - DJANGO=1.2.7

install:
  - pip install -q Django==$DJANGO --use-mirrors

script: python setup.py test

notifications:
  email:
    recipients:
      - bjasper@gmail.com
